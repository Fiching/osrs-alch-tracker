{
  "name": "osrs-alch-tracker",
  "version": "1.0.0",
  "description": "OSRS High Alch Profit Tracker — self-hosted with server-side price caching",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
